services:
  radio-ingestor:
    build: .
    environment:
      - SBS1_SOURCE_HOST=host.docker.internal
      - SBS1_SOURCE_PORT=30003
      - REDIS_HOST
      - REDIS_PORT
      - REDIS_PASSWORD
      - STREAM_KEY
      - STREAM_KEY_SET
